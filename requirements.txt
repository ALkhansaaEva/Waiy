# Tell pip to look at the PyTorch CPU-only package index
--index-url https://download.pytorch.org/whl/cpu

# Install the CPU-only versions (note the "+cpu" suffix)
torch==2.9.0+cpu
torchvision==0.24.0+cpu

# --- Your other packages ---
fastapi==0.121.0
uvicorn==0.38.0
pillow==12.0.0
numpy==2.2.6
typing-extensions>=4.0.0
python-multipart>=0.0.5
timm==0.9.2
opencv-python==4.12.0.88
pydantic==2.12.3